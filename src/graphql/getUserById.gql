query userById($id: Int!){
  userById (id: $id ) {
    id
    username
    firstName
    lastName
    correo
    profiles {
        	id
        	fechaNacimiento
        	genero
        	foto
        	descripcion
        	edad
      }
  }
}
